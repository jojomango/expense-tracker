# 記帳本 — 測試規格 v1

> **本文件是可攜的測試契約。**
> Domain 測案（T1–T3）與 E2E 行為規格（E2E-1~6）在未來改寫 Android 版本時**必須全部保留並重新通過**。
> UI 元件測試不在本文件範圍，視為可拋棄的實作細節。

## 測試分層

| 層 | 位置 | 工具 | 換 Android 時 |
|---|---|---|---|
| Domain 單元 | `tests/domain/` | Vitest | **原封搬走** |
| Persistence | `tests/persistence/` | Vitest + fake-indexeddb | 換儲存引擎時重寫 |
| E2E 行為 | `tests/e2e/` | Playwright | 規格保留，執行器重寫 |
| UI 元件 | `src/ui/**/*.test.tsx` | RTL | 拋棄 |

---

# T1 — Money（金額）

## T1.1 建構與驗證

| # | 條件 | 預期 |
|---|---|---|
| T1.1.1 | `Money.of(10050, 'TWD')` | 內部值 `10050`，顯示 `NT$100.50` |
| T1.1.2 | `Money.of(1000, 'JPY')` | 顯示 `¥1,000`（JPY 0 位小數） |
| T1.1.3 | `Money.of(0, 'TWD')` | 合法，顯示 `NT$0.00` |
| T1.1.4 | `Money.of(-500, 'TWD')` | 合法（餘額可為負），顯示 `-NT$5.00` |
| T1.1.5 | `Money.of(1.5, 'TWD')` | **拋錯** — 最小單位必須是整數 |
| T1.1.6 | `Money.of(100, 'XXX')`（未知幣別） | **拋錯** — 未知幣別代碼 |
| T1.1.7 | `Money.of(Number.MAX_SAFE_INTEGER, 'TWD')` | 合法，不溢位 |

## T1.2 解析使用者輸入

| # | 輸入（TWD） | 預期內部值 |
|---|---|---|
| T1.2.1 | `"100"` | `10000` |
| T1.2.2 | `"100.5"` | `10050` |
| T1.2.3 | `"100.50"` | `10050` |
| T1.2.4 | `"100.567"` | **拋錯** — 超過該幣別小數位 |
| T1.2.5 | `"1,234.56"` | `123456`（容許千分位） |
| T1.2.6 | `""` / `"abc"` / `"-"` | **拋錯** |
| T1.2.7 | `"0"` | `0`（Money 合法，但交易金額驗證會另外拒絕） |
| T1.2.8 | 輸入 `"100.5"`（JPY，0 位小數） | **拋錯** |

## T1.3 運算

| # | 條件 | 預期 |
|---|---|---|
| T1.3.1 | `TWD(10050).add(TWD(2550))` | `TWD(12600)` |
| T1.3.2 | `TWD(10000).subtract(TWD(15000))` | `TWD(-5000)` |
| T1.3.3 | `TWD(100).add(JPY(100))` | **拋錯** — 幣別不符，禁止跨幣運算 |
| T1.3.4 | `sum([])`（空陣列，指定 TWD） | `TWD(0)` |
| T1.3.5 | 連續加 0.1 共 10 次（TWD） | 恰為 `TWD(100)` — **無浮點誤差** |
| T1.3.6 | `TWD(10000).percentOf(TWD(30000))` | `33.33`（回傳 number，僅供顯示） |
| T1.3.7 | `percentOf` 分母為 0 | 回傳 `0`，不得為 `NaN` / `Infinity` |

---

# T2 — Week（週計算）

**這是最容易出錯、也最需要覆蓋的區塊。**

## T2.1 週區間（weekStartDay = 1，週一起）

以 2026-08-11（週二）為基準：

| # | 輸入日期 | 星期 | 預期週區間 |
|---|---|---|---|
| T2.1.1 | `2026-08-11` | 二 | `2026-08-10` ~ `2026-08-16` |
| T2.1.2 | `2026-08-10` | 一 | `2026-08-10` ~ `2026-08-16`（**週首邊界**） |
| T2.1.3 | `2026-08-16` | 日 | `2026-08-10` ~ `2026-08-16`（**週尾邊界**） |
| T2.1.4 | `2026-08-09` | 日 | `2026-08-03` ~ `2026-08-09`（**前一週**） |
| T2.1.5 | `2026-08-17` | 一 | `2026-08-17` ~ `2026-08-23`（**下一週**） |

## T2.2 週區間（weekStartDay = 0，週日起）

| # | 輸入日期 | 星期 | 預期週區間 |
|---|---|---|---|
| T2.2.1 | `2026-08-11` | 二 | `2026-08-09` ~ `2026-08-15` |
| T2.2.2 | `2026-08-09` | 日 | `2026-08-09` ~ `2026-08-15`（週首） |
| T2.2.3 | `2026-08-15` | 六 | `2026-08-09` ~ `2026-08-15`（週尾） |
| T2.2.4 | `2026-08-16` | 日 | `2026-08-16` ~ `2026-08-22` |

## T2.3 週起始日全七種取值

| # | weekStartDay | 基準日 `2026-08-11`（二） | 預期週首 |
|---|---|---|---|
| T2.3.1 | 0 週日 | | `2026-08-09` |
| T2.3.2 | 1 週一 | | `2026-08-10` |
| T2.3.3 | 2 週二 | | `2026-08-11`（當天即週首） |
| T2.3.4 | 3 週三 | | `2026-08-05` |
| T2.3.5 | 4 週四 | | `2026-08-06` |
| T2.3.6 | 5 週五 | | `2026-08-07` |
| T2.3.7 | 6 週六 | | `2026-08-08` |

## T2.4 跨界情境

| # | 條件 | 預期 |
|---|---|---|
| T2.4.1 | 跨月：`2026-07-30`（四），weekStart=1 | `2026-07-27` ~ `2026-08-02` |
| T2.4.2 | 跨年：`2026-01-01`（四），weekStart=1 | `2025-12-29` ~ `2026-01-04` |
| T2.4.3 | 閏年 2 月：`2028-02-29`（二），weekStart=1 | `2028-02-28` ~ `2028-03-05` |
| T2.4.4 | 同一日期、不同 weekStartDay | 回傳不同區間，且**皆包含該日期** |
| T2.4.5 | 任意日期 × 任意 weekStartDay（property test，1000 次隨機） | 區間長度恆為 7 天，且輸入日期恆落在區間內 |
| T2.4.6 | DST 切換日（若使用者裝置為有日光節約的時區） | 區間仍為 7 個曆日，不受時區影響 |

> **實作提示：** 日期一律以 `YYYY-MM-DD` 字串或 UTC 正午的 Date 處理，
> 絕不使用本地時區的 `new Date(y, m, d)` 直接比較，否則 T2.4.6 必掛。

## T2.5 週分組

| # | 條件 | 預期 |
|---|---|---|
| T2.5.1 | 8 筆分屬 3 週的交易，weekStart=1 | 分為 3 組，組內依日期排序，組間依週首倒序 |
| T2.5.2 | 空清單 | 回傳空陣列，不拋錯 |
| T2.5.3 | 全部在同一天 | 1 組 |
| T2.5.4 | 相同資料、weekStartDay 由 1 改為 0 | 分組結果改變（驗證設定確實生效） |

---

# T3 — Budget（預算與餘額）

## T3.1 週預算（budgetMode = weekly）

情境：日常錢包，TWD，週預算 3000.00（`300000`），weekStart=1，今天 `2026-08-11`（二）

| # | 交易 | 預期餘額 |
|---|---|---|
| T3.1.1 | 無交易 | `300000`（NT$3,000.00） |
| T3.1.2 | 本週支出 500 + 300 | `300000 - 80000 = 220000` |
| T3.1.3 | 本週支出 3500 | `-50000`（**超支，負值**），`isOverBudget = true` |
| T3.1.4 | 本週支出 3000（剛好用完） | `0`，`isOverBudget = false`（**邊界：0 不算超支**） |
| T3.1.5 | 本週有一筆收入 5000 | 餘額**不受影響**（D1：只計支出） |
| T3.1.6 | 上週支出 2000，本週 0 | 本週餘額 `300000`（**不結轉、不累計**） |
| T3.1.7 | 交易日期為本週週首當天 | **計入** |
| T3.1.8 | 交易日期為本週週尾當天 | **計入** |
| T3.1.9 | 交易日期為週首前一天 | **不計入** |
| T3.1.10 | 交易日期為週尾後一天 | **不計入** |
| T3.1.11 | 未來日期（本週內）的交易 | **計入**（預先記帳） |
| T3.1.12 | 其他錢包的交易 | **不計入**（錢包隔離） |

## T3.2 總預算（budgetMode = total）

情境：日本旅遊錢包，JPY，總預算 200000

| # | 交易 | 預期 |
|---|---|---|
| T3.2.1 | 無交易 | 餘額 `200000`，已用 `0%` |
| T3.2.2 | 支出 50000 | 餘額 `150000`，已用 `25%` |
| T3.2.3 | 支出跨越多週（3 週共 180000） | 餘額 `20000`，**週界完全無關** |
| T3.2.4 | 支出 250000 | 餘額 `-50000`，已用 `125%`，`isOverBudget = true` |
| T3.2.5 | 有一筆收入 30000 | 餘額**不受影響**（D1） |
| T3.2.6 | 總預算為 0 | 已用百分比回傳 `0`，不得 `NaN` |

## T3.3 無預算（budgetMode = none）

| # | 條件 | 預期 |
|---|---|---|
| T3.3.1 | 呼叫餘額計算 | 回傳 `null`（而非 0），UI 據此隱藏餘額卡片 |
| T3.3.2 | 呼叫本週支出總額 | 仍正常回傳數值 |

## T3.4 多錢包隔離（多幣別關鍵測案）

情境：日常錢包（TWD，週預算 3000）+ 旅遊錢包（JPY，總預算 200000），兩者皆有交易

| # | 條件 | 預期 |
|---|---|---|
| T3.4.1 | 計算日常錢包餘額 | 只含 TWD 交易，回傳 `Money(TWD)` |
| T3.4.2 | 計算旅遊錢包餘額 | 只含 JPY 交易，回傳 `Money(JPY)` |
| T3.4.3 | 任何 API 嘗試加總兩錢包 | **不存在此 API**（規格上不提供） |
| T3.4.4 | 兩錢包餘額計算互不影響 | 修改 JPY 交易後 TWD 餘額不變 |

## T3.5 分類彙總

| # | 條件 | 預期 |
|---|---|---|
| T3.5.1 | 5 筆分屬 3 分類的支出 | 3 組，依金額倒序，含各組佔比 |
| T3.5.2 | 混合收入與支出 | 支出與收入分開彙總，不混算 |
| T3.5.3 | 有交易的分類被刪除 | 該筆歸入「未分類」，交易不遺失 |
| T3.5.4 | 空清單 | 空陣列，佔比計算不 crash |

---

# T4 — Persistence（持久層）

| # | 條件 | 預期 |
|---|---|---|
| T4.1.1 | 新增交易後重新開啟 DB | 資料存在且欄位一致 |
| T4.1.2 | 刪除錢包 | 該錢包所有交易一併刪除，其他錢包不受影響 |
| T4.1.3 | 刪除最後一個錢包 | **被拒絕**，拋出明確錯誤 |
| T4.1.4 | 首次啟動 | 自動建立預設分類（7 支出 + 4 收入） |
| T4.1.5 | Schema v1 → v2 migration | 既有資料完整保留（v1 先寫好 migration 測試骨架） |
| T4.1.6 | 併發寫入同一交易 | 後寫入者勝出，不產生髒資料 |

## T4.2 匯出 / 匯入

| # | 條件 | 預期 |
|---|---|---|
| T4.2.1 | 匯出後立即匯入（replace） | 資料完全一致（round-trip 無損） |
| T4.2.2 | 匯入 schema version 較新的檔 | **拒絕**，提示需更新 app |
| T4.2.3 | 匯入缺少必要欄位的檔 | **整批拒絕**（原子性），現有資料不變 |
| T4.2.4 | 匯入格式錯誤的 JSON | 拋出可讀錯誤，不 crash |
| T4.2.5 | merge 模式，id 衝突 | 保留 `updatedAt` 較新者 |
| T4.2.6 | merge 模式，id 不衝突 | 兩邊資料皆保留 |
| T4.2.7 | 匯入 10,000 筆交易 | 3 秒內完成，UI 不凍結 |
| T4.2.8 | 匯入引用不存在 categoryId 的交易 | **拒絕**（參照完整性檢查） |

---

# E2E — 使用者行為規格

> 以 Given/When/Then 撰寫，**與執行工具無關**。
> Playwright 只是 v1 的執行器；Android 版本需以對應工具重新實作同樣的規格。

## E2E-1 首次使用

```
Given 使用者第一次開啟 app（無任何資料）
When  畫面載入
Then  顯示引導畫面，要求建立第一個錢包

When  輸入名稱「日常」、幣別「TWD」、預算模式「每週」、金額「3000」
And   送出
Then  進入主畫面
And   顯示錢包「日常」
And   顯示本週餘額「NT$3,000.00」
And   分類清單已預先填入 7 個支出分類與 4 個收入分類
```

## E2E-2 記一筆帳

```
Given 已有錢包「日常」（TWD，週預算 3000）且本週無交易
When  點擊新增按鈕
And   輸入金額「120」、選擇分類「飲食」、日期保持預設（今天）
And   送出
Then  交易出現在列表最上方，顯示「NT$120.00」與「🍜 飲食」
And   本週餘額立即變為「NT$2,880.00」（**無需重新整理**）

When  將該筆金額編輯為「200」
Then  本週餘額立即變為「NT$2,800.00」

When  刪除該筆交易
Then  交易從列表消失
And   本週餘額立即回到「NT$3,000.00」
```

## E2E-3 超支警示

```
Given 錢包「日常」週預算 3000，本週已支出 2900
When  新增一筆支出 500
Then  本週餘額顯示「-NT$400.00」
And   餘額以警示樣式呈現（紅色 + 警示圖示）
And   畫面明確標示「已超支」
```

## E2E-4 多錢包與多幣別

```
Given 已有錢包「日常」（TWD，週預算 3000，本週已支出 500）
When  建立第二個錢包「日本旅遊」，幣別「JPY」，預算模式「總預算」，金額「200000」
And   切換至「日本旅遊」
Then  顯示剩餘總預算「¥200,000」（**無小數點**）
And   交易列表為空

When  在「日本旅遊」新增支出「¥8,000」分類「飲食」
Then  剩餘總預算顯示「¥192,000」，已用「4%」

When  切換回「日常」
Then  本週餘額仍為「NT$2,500.00」（**完全不受 JPY 交易影響**）
And   畫面任何位置皆不出現跨幣別的合計數字
```

## E2E-5 週起始日設定

```
Given weekStartDay 為「週一」，今天是 2026-08-11（週二）
And   在 2026-08-09（週日）有一筆支出 1000
And   在 2026-08-10（週一）有一筆支出 500
Then  本週餘額為「NT$2,500.00」（只計週一那筆）

When  將 weekStartDay 改為「週日」
Then  本週餘額立即變為「NT$1,500.00」（兩筆都計入）
And   交易列表的週分組標題同步更新
And   **沒有任何交易資料被修改**（金額與日期不變）
```

## E2E-6 備份與還原

```
Given 已有 2 個錢包與 15 筆交易
When  在設定中點擊「匯出備份」
Then  下載一個 JSON 檔

Given 使用者清除瀏覽器資料（模擬換裝置）
When  開啟 app 並匯入該備份檔（replace 模式）
Then  2 個錢包與 15 筆交易完整還原
And   所有餘額計算結果與匯出前完全一致
And   週起始日等設定一併還原
```

## E2E-7 離線與 PWA

```
Given app 已在瀏覽器開啟過一次
When  裝置切換為飛航模式
And   重新開啟 app
Then  app 正常載入
And   所有既有資料可讀取
And   可正常新增交易
And   期間沒有任何網路請求失敗的錯誤

When  在 Android Chrome 開啟部署後的網址
Then  瀏覽器提供「加到主畫面」選項
And   安裝後以獨立視窗（無網址列）啟動
```

---

# 測試品質門檻（CI 強制）

| 項目 | 門檻 | 失敗處理 |
|---|---|---|
| `src/domain/` 行覆蓋率 | ≥ 90% | build 失敗 |
| `src/domain/` 外部 import | 0 | build 失敗 |
| TypeScript strict 錯誤 | 0 | build 失敗 |
| ESLint error | 0 | build 失敗 |
| 本文件所有測案 | 全數通過 | build 失敗 |
| E2E 於 Chromium + Mobile Chrome viewport | 全數通過 | build 失敗 |

---

# 給 Agent 的測試撰寫規則

1. **測案編號必須出現在測試名稱中**，例如
   `it('T2.1.4 — 週日應歸屬前一週', ...)`。
   這樣 CI 報告能直接對回本文件。
2. Domain 測試**不得** mock 任何東西 — 純函式無須 mock。若需要 mock，代表設計違反了 P1。
3. 時間相關測試必須注入固定的 `referenceDate`，**禁止依賴 `new Date()`**。
4. E2E 測試必須從乾淨的 IndexedDB 開始，每個測試自行建立前置資料。
5. 新增本文件未涵蓋的測案是**允許且鼓勵的**，但不得刪除或弱化既有測案。
   若認為某測案有誤，在 PR 中提出並暫停，不得自行修改。
